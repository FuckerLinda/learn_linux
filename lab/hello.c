#include <math.h>
#include <stdio.h>

int main() {
    double result = sqrt(2.0);
    int a=3;
    printf("Square root: %f\n", result);
    for (;a<10;a++){
	    printf("a=%d",a);
    }
    return 0;
}
